version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "Installing dependencies..."
            - pip install -r requirements.txt
        build:
          commands:
            - echo "Build phase is not needed for Streamlit, skipping..."
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
    appRoot: .
    platform: WEBSERVER